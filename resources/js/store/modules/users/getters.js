export const users = state => {
    return state.users
}

export const usersWithPaginate = state => {
    return state.usersWithPaginate
}

export const user = state => {
    return state.user
}

export const usersLength = state => {
    return state.users.length
}

export const infos = state => {
    return state.infos
}

export const usersWithDate = state => {
    return state.usersWithDate
}
